# CDX Batch Python Starter

This is a minimal starter kit to:
- package a Python script into a Docker image
- run it as a CDX Batch job
- pass a request file path as a Batch input variable
- write outputs and upload them

Start by running `hello_batch.py` (no Oracle calls) to validate plumbing.
Then swap `PROGRAM` to your real FA transfer script.
